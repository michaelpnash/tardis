package domain

import org.specs2.mutable._

class EventRouterSpec extends Specification {
  "The event router" should {
    "record a received event in the event store" in {
       pending
    }
  }
}