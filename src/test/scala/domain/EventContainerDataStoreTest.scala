package domain

import org.scalatest.FunSpec

class EventContainerDataStoreTest extends FunSpec {
  describe("The event container data store") {
    it ("should save a new event container data store entity") (pending)
  }  
}
